<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ncit.firebaselogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BullRent">
        <activity
            android:name="com.ncit.firebaselogin.SignUp"
            android:exported="false" />
        <activity
            android:name="com.ncit.firebaselogin.Login"
            android:exported="false" />
        <activity
            android:name="com.ncit.firebaselogin.Dashboard"
            android:exported="false" />
        <activity
            android:name="com.ncit.firebaselogin.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>